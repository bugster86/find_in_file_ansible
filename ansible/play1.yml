---
- hosts: all
  gather_facts: false

  tasks:
    - name: uso il modulo custom
      my_test:
        file: '/etc/hosts'
        stringa: 'ip6'
      notify: stampa_trovato

  handlers:
    - name: stampa_trovato
      debug: 
        msg: "La stringa esiste nel file" 
